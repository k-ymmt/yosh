/target
crates/*/target
tests/**/target

# cargo-component regenerates these on every build (WIT package version
# is embedded); the test plugins get their guest bindings from the SDK's
# wit_bindgen::generate! macro instead, so these files are never compiled.
tests/plugins/*/src/bindings.rs

/.claude/settings.local.json
/.claude/scheduled_tasks.lock
mise.local.toml

# Git worktrees
.worktrees/

# OS
.DS_Store
Thumbs.db

# Editors / IDEs
.vscode/
.idea/
*.swp
*.swo
*~
*.bak
*.orig

# Environment / secrets
.env
.env.*
