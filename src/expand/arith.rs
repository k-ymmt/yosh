use crate::env::ShellEnv;

/// Evaluate an arithmetic expression and return the result as a string.
/// Phase 3 stub: always returns "0".
pub fn evaluate(_env: &mut ShellEnv, _expr: &str) -> String {
    "0".to_string()
}
