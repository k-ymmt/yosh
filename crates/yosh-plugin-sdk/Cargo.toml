[package]
name = "yosh-plugin-sdk"
version = "0.1.5"
edition = "2024"
description = "SDK for building yosh shell plugins"
license = "MIT"
repository = "https://github.com/k-ymmt/yosh"

[dependencies]
yosh-plugin-api = { version = "0.2.0", path = "../yosh-plugin-api" }

[lib]
crate-type = ["rlib"]
